# 初轮基准（2026-09-14）

环境：Apple Silicon macOS 26.6.1，Gifsicle 1.96，同版本未修改 CLI 作为参考。合成数据由 `tool/generate_fixtures.py` 确定性生成；没有读取业务图片。

| 样本 | 输入 / 输出字节 | 原生中位耗时（5 次） | 原版 CLI 进程墙钟 |
| --- | ---: | ---: | ---: |
| 小型静态图 | 930 / 140 | 0.222 ms | 5.781 ms |
| 小型动画 | 8034 / 1776 | 0.346 ms | 3.630 ms |
| 512×512、3 帧 | 118648 / 115668 | 12.824 ms | 17.731 ms |
| 24×24、100 帧 | 92422 / 15615 | 1.580 ms | 6.705 ms |
| 多颜色动画 | 30762 / 23339 | 2.586 ms | 6.500 ms |
| 透明动画 | 8092 / 1842 | 0.360 ms | 4.695 ms |
| 混合 disposal / 局部调色板 | 2833 / 505 | 0.231 ms | 4.214 ms |

这些样本的无损输出与原版 CLI 字节一致；完整解码后的帧数、画布、逐帧 RGBA、delay 和 loop 一致。透明像素的不可见 RGB 归零后比较，保留 alpha；Gifsicle 优化可能改变存储矩形和 disposal 表达，因此这里验证的是合成后的视觉和时序语义。

有损 `--lossy=20` 在这批几何合成样本上恰好没有引入可见差异（MAE=0）。这不能证明真实照片的有损质量，仍需要更丰富的高熵/照片类自有样本。测试设置的逐通道平均误差门槛为 12/255，仅用于这些明确的 Fixture。

100 次重复转换前及第 1、25、50、75、100 次观测 RSS 均为 46688 KiB，没有观测到持续线性增长。另有带分配失败注入、并发和重复错误路径的 ASan/UBSan/LeakSanitizer 原生测试。RSS 样本不等价于完整泄漏证明。

CLI 墙钟包含进程启动，不能把表中差值解释成算法加速。原生计时排除了 Dart 数据传输和工作目录暂存；已补充下表的 Dart 总耗时；独立传输成本、移动端峰值 RSS、处理后回落及更大样本阈值仍需补测。因此本轮只建立观测基线，不声明达到全部 1.0 性能门槛。

原始记录：`tool/reports/benchmark.json`。复测命令：

```sh
python3 tool/generate_fixtures.py
python3 tool/native_build.py library
python3 tool/benchmark.py /path/to/unmodified/gifsicle-1.96
```


## Dart Worker 初轮观测

`dart run tool/dart_benchmark.dart`，预热后记录，单位微秒。纯字节转换直接传入原生内存流，不创建暂存目录。

| 样本 | 调用总耗时 | Worker 内耗时 | Worker 外差值 |
| --- | ---: | ---: | ---: |
| 小型动画 | 845 | 590 | 255 |
| 大画布 | 14581 | 14393 | 188 |
| 多帧动画 | 1999 | 1862 | 137 |

差值包含消息传输、调度及封装，不能解释为单独的内存复制成本。原始记录：`tool/reports/dart_benchmark.json`。
